const content = {
    display: 'flex',
    flexDirection: 'column',
    position: 'absolute',
    top: '15%',
    height: '60%',
    margin: 'var(--comm-margin)',
    borderRadius: 'var(--comm-border-radius)',
}

const fontStyle = {
    fontSize: '1.3rem',
    width: '10%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
}

const inputStyle = {
    fontFamily: 'var(--comm-font-family)',
    fontSize: '1.8rem',
    backgroundColor: 'var(--comm-bg-sky-blue)',
    borderRadius: '5px',
    border: '0',
    outline: '0',
    right: '10%',
    width: '90%',
}

const rowWrapper = {
    marginTop: '3px',
    height: '20%',
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
}

const rowWrapper2 = {
    marginTop: '3px',
    height: '20%',
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
}

const selectStyle = {
    fontFamily: 'var(--comm-font-family)',
    fontSize: '1.8rem',
    backgroundColor: 'rgba(150, 150, 150, 0.5)',
    borderRadius: '5px',
    border: '0',
    outline: '0',
    right: '10%',
    width: '90%',
}

const addButtonStyle = {
    fontSize: '1.2rem',
    fontFamily: 'var(--comm-font-family)',
    height: '60%',
    outline: '0',
    backgroundColor: 'var(--comm-bg-sky-blue)',
    border: '2px solid rgba(163, 185, 184, 0.2)',
    borderRadius: '3px',
    width: '30%',
}

const closeButtonStyle = {
    fontSize: '1.2rem',
    fontFamily: 'var(--comm-font-family)',
    marginLeft: '3px',
    height: '60%',
    outline: '0',
    backgroundColor: 'rgba(238, 139, 154, 1)',
    border: '2px solid rgba(163, 185, 184, 0.2)',
    borderRadius: '3px',
    width: '30%',
}

export { content, fontStyle, inputStyle, rowWrapper, rowWrapper2, selectStyle, addButtonStyle, closeButtonStyle };